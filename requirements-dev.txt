# ============================================================
# Developer-only dependencies (NOT installed in production).
# ============================================================
# هر ابزار اینجا فقط برای کیفیت کد، تست، یا توسعه local لازم است.
# Production image باید فقط requirements.txt را نصب کند.

# ── Code quality ───────────────────────────────────────────
ruff==0.14.4

# ── Testing ────────────────────────────────────────────────
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
factory-boy==3.3.1
responses==0.25.7